---
- name: This is a hello-world example
  hosts: all
  tasks:
   - name: create multiple files
     copy:
      #dest: "{{ item }}"
      dest: /tmp/"{{ item }}"
      
      content: |
         It is sample file
      with_items:
        -  Test1.txt
        -  Test2.txt
        -  Test3.txt
        -  Test4.txt
        -  Test5.txt
        -  Test6.txt

        
        